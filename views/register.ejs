<%#
I declare that this code was written by me.
I will not copy or allow others to copy my code.
Student Name: Zhang KaiXiang
Student ID: 24041976
Class: C372-002
Date created: 06/02/2026
%>


<%- include('partials/header', { title: 'Register | Shirt Shop' }) %>
  <%- include('partials/navbar', { user: typeof user !=='undefined' ? user : null }) %>

    <section class="page-section">
      <div class="container">
        <div class="form-card">
          <h1 class="text-center">Create your account</h1>
          <p class="text-center text-muted mb-4">Join Shirt Shop to access curated shirts, exclusive drops, and seamless
            checkout.</p>

          <% if (messages && messages.length) { %>
            <div class="alert alert-danger">
              <% messages.forEach(function(message) { %>
                <p class="mb-1">
                  <%= message %>
                </p>
                <% }); %>
            </div>
            <% } %>

              <form action="/register" method="POST" class="mt-4">
                <div class="mb-3">
                  <label for="username" class="form-label">Username</label>
                  <input type="text" id="username" name="username" class="form-control" placeholder="Jane Doe" required
                    value="<%= formData && formData.username ? formData.username : '' %>">
                </div>
                <div class="row g-3">
                  <div class="col-12 col-md-6">
                    <div class="mb-3">
                      <label for="first_name" class="form-label">First name</label>
                      <input type="text" id="first_name" name="first_name" class="form-control" placeholder="Jane"
                        value="<%= formData && formData.first_name ? formData.first_name : '' %>">
                    </div>
                  </div>
                  <div class="col-12 col-md-6">
                    <div class="mb-3">
                      <label for="last_name" class="form-label">Last name</label>
                      <input type="text" id="last_name" name="last_name" class="form-control" placeholder="Doe"
                        value="<%= formData && formData.last_name ? formData.last_name : '' %>">
                    </div>
                  </div>
                </div>
                <div class="mb-3">
                  <label for="email" class="form-label">Email address</label>
                  <input type="email" id="email" name="email" class="form-control" placeholder="you@example.com"
                    required value="<%= formData && formData.email ? formData.email : '' %>">
                </div>
                <div class="mb-3">
                  <label for="password" class="form-label">Password</label>
                  <input type="password" id="password" name="password" class="form-control"
                    placeholder="Create a secure password" required>
                  <div class="form-check mt-2">
                    <input class="form-check-input" type="checkbox" id="toggleRegisterPassword" data-toggle-password
                      data-target="password">
                    <label class="form-check-label" for="toggleRegisterPassword">Show password</label>
                  </div>
                </div>
                <div class="mb-3">
                  <label for="address" class="form-label">Address</label>
                  <input type="text" id="address" name="address" class="form-control"
                    placeholder="123 Style Avenue" required
                    value="<%= formData && formData.address ? formData.address : '' %>">
                </div>
                <div class="row g-3">
                  <div class="col-12 col-md-4">
                    <div class="mb-3">
                      <label for="city" class="form-label">City</label>
                      <input type="text" id="city" name="city" class="form-control" placeholder="City"
                        value="<%= formData && formData.city ? formData.city : '' %>">
                    </div>
                  </div>
                  <div class="col-12 col-md-4">
                    <div class="mb-3">
                      <label for="state" class="form-label">State</label>
                      <input type="text" id="state" name="state" class="form-control" placeholder="State"
                        value="<%= formData && formData.state ? formData.state : '' %>">
                    </div>
                  </div>
                  <div class="col-12 col-md-4">
                    <div class="mb-3">
                      <label for="zip_code" class="form-label">ZIP / Postal</label>
                      <input type="text" id="zip_code" name="zip_code" class="form-control" placeholder="123456"
                        value="<%= formData && formData.zip_code ? formData.zip_code : '' %>">
                    </div>
                  </div>
                </div>
                <div class="mb-3">
                  <label for="country" class="form-label">Country</label>
                  <input type="text" id="country" name="country" class="form-control" placeholder="Country"
                    value="<%= formData && formData.country ? formData.country : '' %>">
                </div>
                <div class="mb-3">
                  <label for="phone" class="form-label">Phone Number</label>
                  <input type="text" id="phone" name="phone" class="form-control" placeholder="(+65) 1234 5678" required
                    value="<%= formData && formData.phone ? formData.phone : '' %>">
                </div>
                <div class="mb-4">
                  <label for="role" class="form-label">Role</label>
                  <select id="role" name="role" class="form-select">
                    <option value="customer" <%=formData && formData.role==='customer' ? 'selected' : '' %>>Customer
                    </option>
                    <option value="admin" <%=formData && formData.role==='admin' ? 'selected' : '' %>>Admin</option>
                  </select>
                </div>
                <button type="submit" class="btn btn-primary w-100">Create account</button>
              </form>
              <p class="text-center text-muted mt-4 mb-0">Already have an account? <a href="/login">Sign in</a></p>
        </div>
      </div>
    </section>

    <script>
      document.addEventListener('DOMContentLoaded', function () {
        document.querySelectorAll('[data-toggle-password]').forEach(function (toggle) {
          var target = document.getElementById(toggle.dataset.target);
          if (!target) {
            return;
          }
          toggle.addEventListener('change', function () {
            target.type = toggle.checked ? 'text' : 'password';
          });
        });
      });
    </script>
    <%- include('partials/footer') %>
