<% const extraHead=` <style>
  .hero-card {
  margin-top: 3rem;
  margin-bottom: 3rem;
  }

  .hero-image {
  max-height: 340px;
  object-fit: contain;
  }

  @media (max-width: 767px) {
  .hero-card {
  padding: 1.4rem 0.8rem;
  }

  .hero-image {
  max-height: 200px;
  }
  }
  </style>
  `;
  %>
  <%- include('partials/header', { title: 'Shirt Shop' , extraHead }) %>
    <%- include('partials/navbar', { user: typeof user !=='undefined' ? user : null }) %>

      <section class="page-section">
        <div class="container">
          <div class="hero-card">
            <div class="row g-4 align-items-center">
              <div class="col-12 col-lg-6">
                <div class="hero-body">
                  <% if (!user) { %>
                    <h1 class="mb-3">Welcome to Shirt Shop</h1>
                    <p class="lead text-muted mb-4">Shop quality shirts, discover new styles, and enjoy a smooth
                      checkout
                      experience. Find your next favourite tee today.</p>
                    <div class="hero-cta">
                      <a href="/register" class="btn btn-primary btn-lg">Create an account</a>
                      <a href="/login" class="btn btn-secondary btn-lg">Sign in</a>
                    </div>
                    <% } else if (user.role==='admin' ) { %>
                      <h1 class="mb-3">Hello, <%= user.username %>!</h1>
                      <p class="lead text-muted mb-4">Manage inventory, oversee your catalogue, and keep shoppers
                        supplied
                        with the latest shirts.</p>
                      <div class="hero-cta">
                        <a href="/inventory" class="btn btn-primary btn-lg">Inventory</a>
                        <a href="/admin/users" class="btn btn-secondary btn-lg">Manage Users</a>
                      </div>
                      <% } else { %>
                        <h1 class="mb-3">Welcome back, <%= user.username %>!</h1>
                        <p class="lead text-muted mb-4">Discover new styles and seasonal drops—your next favourite shirt
                          awaits!</p>
                        <div class="hero-cta">
                          <a href="/shopping" class="btn btn-primary btn-lg">Start Shopping</a>
                          <a href="/cart" class="btn btn-secondary btn-lg">View Cart</a>
                        </div>
                        <% } %>
                </div>
              </div>
              <div class="col-12 col-lg-6">
                <div class="hero-image-wrapper">
                  <img src="https://i.makeagif.com/media/zVu4hG.gif"
                    onerror="this.onerror=null;this.src='https://media1.tenor.com/m/NnxNmhvQNqwAAAAd/shopping.gif';"
                    alt="Stylish shirts for everyone!" class="img-fluid rounded-4 shadow hero-image" loading="lazy">
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <%- include('partials/footer') %>